import QtQuick
import Quickshell
import Quickshell.Io
import qs.Commons

Scope {
    id: root
    
    // We can define the overlay in the panelWindow if we want to cover the screen,
    // but panelWindow is small! It's a pill shape at the top!
    // To cover the screen, we need a Quickshell Window!
}
